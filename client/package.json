{
  "name": "client",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run buildCSS && node build.js",
    "buildCSS": "npx tailwindcss -i src/style.css -o src/content/includes/_temp/min.css --minify",
    "local": "netlify dev"
  },
  "dependencies": {
    "@11ty/eleventy": "^1.0.2",
    "fs-extra": "^10.1.0",
    "netlify-cli": "^12.0.7",
    "tailwindcss": "^3.1.8"
  }
}
